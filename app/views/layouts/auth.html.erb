<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Knowledge Service" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Knowledge Service">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <%= favicon_link_tag "branding/favicon.ico", sizes: "any" %>
    <%= favicon_link_tag "branding/favicon-32x32.png", type: "image/png", sizes: "32x32" %>
    <%= favicon_link_tag "branding/favicon-16x16.png", type: "image/png", sizes: "16x16" %>
    <%= tag.link rel: "apple-touch-icon", href: image_path("branding/apple-touch-icon.png") %>

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="container mx-auto mt-28 px-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
